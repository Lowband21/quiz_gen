Question: What is the main difference between singly linked lists and doubly linked lists?
Possible Answers: 
a) Singly linked lists can be traversed in both directions, while doubly linked lists can only be traversed in one direction.
b) Singly linked lists have nodes with references to the next node, while doubly linked lists have nodes with references to both the next and the previous node.
c) Singly linked lists have a reference to the last node in the list, while doubly linked lists have a reference to the first node in the list.
d) Singly linked lists use sentinel nodes, while doubly linked lists do not.
Key: b