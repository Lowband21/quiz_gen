Question: What are the key characteristics of a tuple in Python?
Possible Answers: 
a) Tuples are mutable and can contain multiple variables of the same type.
b) Tuples are mutable and can contain multiple variables of different types.
c) Tuples are immutable and can contain multiple variables of the same type.
d) Tuples are immutable and can contain multiple variables of different types.
Key: d