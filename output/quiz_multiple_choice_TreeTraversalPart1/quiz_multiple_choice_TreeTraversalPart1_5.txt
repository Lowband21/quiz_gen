1. Question: What is the order in which nodes are visited in a pre-order traversal of a tree?
Possible Answers:
a) Process the root node, then the left child, then the right child
b) Process the left child, then the root node, then the right child
c) Process the left child, then the right child, then the root node
d) Process the right child, then the left child, then the root node
Key: a
