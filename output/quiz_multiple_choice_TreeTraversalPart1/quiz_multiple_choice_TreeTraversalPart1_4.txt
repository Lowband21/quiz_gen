1. Question: What is the order in which nodes are visited in a pre-order traversal of a tree?

Possible Answers:
a) Root node, left subtree, right subtree
b) Left subtree, root node, right subtree
c) Left subtree, right subtree, root node
d) Right subtree, left subtree, root node

Key: a
